<% content_for :title, "コースを編集 | Pitat" %>

<div class="container py-5" style="max-width: 480px;">
  <div class="d-flex align-items-center mb-4 gap-2">
    <%= link_to mobile_library_path(@mobile_library), class: "text-muted" do %>
      <i class="bi bi-chevron-left"></i>
    <% end %>
    <h1 class="fs-5 fw-bold mb-0"><%= @mobile_library.name %> コースを編集</h1>
  </div>
  <div class="pitat-card p-4">
    <%= render "form", mobile_library: @mobile_library, route: @route %>
  </div>
</div>
